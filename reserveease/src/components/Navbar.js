import React from "react";

// PUBLIC_INTERFACE
function Navbar() {
  /**
   * Navbar - fixed top navigation bar with brand and navigation links.
   * Stub – To be implemented.
   */
  return <nav>Navbar Component (skeleton)</nav>;
}

export default Navbar;
